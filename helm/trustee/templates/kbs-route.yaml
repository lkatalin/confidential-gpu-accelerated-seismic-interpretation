apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: kbs-service
  namespace: trustee-operator-system
spec:
  port:
    targetPort: kbs-port
  tls:
    termination: passthrough
  to:
    kind: Service
    name: kbs-service
